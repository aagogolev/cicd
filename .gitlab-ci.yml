stages:
  - build

build:
  stage: build
  image: docker:latest
  script:
    - echo Building project 
    - echo $CI_REGISTRY_USER
    - echo $CI_REGISTRY_PASSWORD
    - echo $CI_REGISTRY
    #- docker login -u "$CI_REGISTRY_USER" -p efE3_1sCWzy8ropNTyr2 $CI_REGISTRY
    - docker login -u "$CI_REGISTRY_USER" -p ${CI_JOB_TOKEN} ${CI_REGISTRY}
    - echo login
    - docker build -t gitlab.ru:4567/alexandr_gogolev/cicd/cicd/
    - echo build 
    - docker push gitlab.ru:4567/alexandr_gogolev/cicd
    - echo push
  tags:
    - docker  
